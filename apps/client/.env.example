# URL of the deployed Colyseus game server (no trailing slash).
# Local dev can leave this unset — the client defaults to ws://<host>:2567.
# Production example (Railway): wss://your-app.up.railway.app
NEXT_PUBLIC_SERVER_URL=
